import styled from "styled-components";

// Styled Component named DivStyle

const divStyle = {
    margin: 'auto',
    border: '1px solid rgba(255,255,255,0.20)'
};


export default divStyle;

const buttonStyle = {
    margin: 'auto',
}

export default buttonStyle;


const styles = StyleSheet.create({
    
})



